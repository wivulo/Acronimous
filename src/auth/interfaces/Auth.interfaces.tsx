export interface RegisterItemProps{
    children: any;
    item: number;
    activeItm: number;
}

export interface ButtonProps {
    content: any;
    eventHandler: any;
    isDisabled: Boolean;
    id: string;
}